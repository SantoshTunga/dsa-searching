# Compiled output
target/
build/
bin/

# IntelliJ IDEA project files
.idea/
*.iml
out/

# Eclipse project files
.classpath
.project
.settings/

# NetBeans project files
nbproject/

# Maven build output (redundant with target/ but kept for clarity)
target/

# Gradle build output (redundant with build/ but kept for clarity)
.gradle/
build/

# Log files
*.log
logs/

# Package files (JARs, WARs, etc.)
*.jar
*.war
*.ear
*.zip
*.gz

# Spring Boot DevTools restart file
spring-boot-devtools.properties

# OS-specific files and folders
.DS_Store
Thumbs.db

# Temporary files and directories
tmp/
temp/
*.tmp
*.bak
*.swp
*.swo
.gradle/caches/build-cache-1

# Dependency management specific (if applicable)
dependency-updates/
resolution-rules/
gradle-diff/

# Other build/tool specific
.mvn/wrapper/maven-wrapper.jar
.sonarlint/

# Files generated by annotation processors
generated/

# Executable files
*.exe
*.out

# Keystore files (if you don't want to track them)
*.jks
*.keystore

# Environment specific files (consider if you want to track these)
.env
*.properties.local
*.yml.local
application-local.properties
application-local.yml

# Report files
build/reports/

# Docker related (if applicable)
docker-compose.override.yml
docker-compose.prod.yml

# Terraform state (if applicable)
.terraform/
terraform.tfstate
terraform.tfstate.*

# Node.js dependencies and build output (if frontend is included)
node_modules/
dist/
yarn-error.log

# Python virtual environment (if Python is used for scripting)
venv/
__pycache__/
*.pyc

# More generic ignores
*~ # Backup files ending in ~
\#*# # Emacs backup files
.*.swp # Vim swap files
.cache/

# Class files (explicitly ignoring, though often covered by target/build)
*.class

# XML files (consider if you want to ignore all or specific ones)
# *.xml
# config/*.xml
# src/main/resources/config/*-local.xml